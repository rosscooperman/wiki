<div id="page-header">
  <h1><%= @page.title %></h1>
  <div class="tools">
    <%= link_to 'Edit Page', edit_page_path(@page), :class => 'edit' %>
    <%= delete_link 'Delete Page', page_path(@page) %>
  </div>
</div>

<div id="page-content"><%= @page.content %></div>
